#include<stdio.h>
int main()
{
    int a,b,T,i,l;
    float sum;
    scanf("%d",&T);
    for(i=1;i<=n;i++)
    {
        sum=0;
        gets(a);
        l=strlen(a);
        if(a[l-1]=='b')
        [
            sum+=(a[l-2]-48)*.05;
            sum+=
    }
}
