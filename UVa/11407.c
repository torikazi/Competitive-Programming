#include<stdio.h>
int main()
{
    long long int t,n,i,j,a,c;
    scanf("%lld",&t);
    for(i=1;i<=t;i++)
    {
        scanf("%lld",&n);
        c=0;
        x=sqrt(n);
        while(n!=0)
        {
            for(j=x;j*j<=n)
        }
        printf("%lld\n",c);
    }
    return 0;
}

